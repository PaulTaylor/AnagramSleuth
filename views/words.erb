
<header class="col-md-12">
  <h1>Results of Anagram Search</h1>
  <p>We found <%= result.size %> words within <%= input %>.</p>
</header>

<article class="col-md-12">
  <div class="col-sm-10 col-sm-offset-1">
    <table class="table">
      <thead>
        <tr>
          <th>Word</th>
          <th>Length</th>
        </tr>
      </thead>
      <tbody>
        <% result.each do |word| %>
          <tr>
            <td><%= word %></td>
            <td><%= word.size %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <p class="col-sm-12">
    Generating the list of words took <%= time %> seconds.
  </p>
</article>
