
<header class="col-md-12">
  <h3>Result</h3>
</header>

<article class="col-md-12">
  <% if formula then %>
    <div class="alert bg-primary lead">
      <%= formula.gsub(/([\*\/\+\-])/, ' \1 ').gsub(/\*|\]/, { '/' => '&division;', '*' => '&times;' }) %>
    </div>
  <% else %>
    <div class="alert bg-danger">
      No result found :(
    </div>
  <% end %>

  <p>
    Generating the result took <%= time %> seconds.
  </p>
</article>
